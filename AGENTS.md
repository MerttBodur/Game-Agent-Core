# Agent Rules

- Read `replit.md` before starting any project work.
- Treat `lib/api-spec/openapi.yaml` as the API source of truth.
- Use OpenAI Docs MCP for OpenAI API, embeddings, RAG, model, and structured output work.
- Use Context7 for current library documentation.
- MCP in this phase is development tooling only.
- Runtime RAG implementation belongs under `artifacts/api-server/src/lib` in a later phase.
